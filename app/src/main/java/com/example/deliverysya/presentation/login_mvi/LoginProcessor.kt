package com.example.deliverysya.presentation.login_mvi

import android.app.Activity
import android.content.ContentValues
import android.util.Log
import android.widget.Toast
import com.google.firebase.auth.FirebaseAuth
import kotlinx.coroutines.flow.Flow

internal class LoginProcessor {

    /*fun actionProcessor(actions: LoginAction): Flow<LoginResult> =
        when (actions) {
        /    is LoginAction.GetSingWhitEmailAndPassword -> SingWhitEmailAndPassword(actions.user,actions.Password,actions.activity)
        }*/
}

//private fun SingWhitEmailAndPassword(user: String, pass: String,activity: Activity): Flow<LoginResult> =
